server:
  port: ${PORT:8080}
  address: 0.0.0.0

spring:
  jpa:
    hibernate:
      ddl-auto: update

app:
  seed:
    enabled: ${APP_SEED_ENABLED:true}
  uploads:
    dir: ${APP_UPLOADS_DIR:/app/uploads}
